import { ActionType } from "../../models";
import * as React from "react";

interface ActionTypesProperties {
    type: ActionType;
}

export default class ActionTypes extends React.Component<ActionTypesProperties, any> {
}